{
  "name": "wiki-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for the SQLite-backed software wiki (pages, links, log, FTS + semantic search)",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "bin": {
    "wiki-cli": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "migrate": "node dist/migrate.js",
    "cli": "node dist/cli.js"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.7.0"
  }
}
